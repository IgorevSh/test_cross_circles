# Крестики-нолики в Telegram-боте

Игра "Крестики-нолики" с гибридным подходом! Играйте прямо в боте или на красивом сайте - на выбор!

## Структура проекта

- `frontend/` - HTML/CSS/JS приложение игры с красивым дизайном
- `backend/` - NestJS сервер с Telegram ботом

## Установка и настройка

### 1. Настройка Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot` и следуйте инструкциям
3. После создания бота вы получите **токен бота** (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Примечание:** `TELEGRAM_CHAT_ID` больше не обязателен, так как игра работает прямо в боте. Но если вы хотите использовать старые методы API для отправки сообщений, можете оставить его.

### 2. Установка зависимостей

Все зависимости находятся в корневом `package.json`, поэтому достаточно одной команды:

```bash
npm install
```

Эта команда установит все зависимости (и для backend, и для frontend) в корневую папку `node_modules`.

### 3. Настройка переменных окружения

Создайте файл `.env` в папке `backend/`:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
FRONTEND_URL=http://localhost:8080
```

Пример:
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
FRONTEND_URL=http://localhost:8080
```

**Примечания:**
- `TELEGRAM_BOT_TOKEN` - обязателен для работы бота
- `FRONTEND_URL` - URL вашего фронтенда (по умолчанию `http://localhost:8080`)
- `TELEGRAM_CHAT_ID` - опционален, нужен только для совместимости со старым API

### 4. Запуск приложения

**⭐ Самый простой способ - запуск одной командой:**

```bash
npm start
```

Эта команда запустит одновременно:
- ✅ Backend на `http://localhost:3000`
- ✅ Frontend на `http://localhost:8080`

**Альтернативные способы запуска:**

Запуск по отдельности:
```bash
# Только backend
npm run start:backend

# Только frontend
npm run start:frontend
```

Или вручную:
```bash
# Backend
cd backend
npm run start:dev

# Frontend (в другом терминале)
cd frontend
python -m http.server 8080
# или
npx http-server -p 8080
```

### 5. Запуск игры

После запуска backend сервера:

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start` - увидите доступные опции:
   - **🎮 /game** - Играть прямо в боте (inline-кнопки)
   - **🌐 /site** - Получить ссылку на веб-версию игры
3. Выберите удобный способ игры!

## Как это работает

### Вариант 1: Игра в боте (`/game`)

1. **Начните игру** - отправьте команду `/game` боту
2. **Игрок делает ход** - нажмите на любую свободную клетку (⬜) в игровом поле
3. **Компьютер отвечает** - автоматически делает свой ход
4. **При победе игрока**:
   - В игровом поле отображается сообщение о победе
   - Генерируется и показывается случайный 5-значный промокод
   - Отправляется отдельное сообщение с промокодом
5. **При проигрыше**:
   - В игровом поле отображается сообщение о проигрыше
   - Отправляется уведомление "Проигрыш"
   - Появляется кнопка "Играть ещё раз"
6. **Новая игра** - нажмите кнопку "🎮 Играть ещё раз" или отправьте `/game`

### Вариант 2: Игра на сайте (`/site`)

1. **Получите ссылку** - отправьте команду `/site` боту
2. **Откройте ссылку** - нажмите на кнопку "🎮 Открыть игру" или скопируйте ссылку
3. **Играйте на сайте** - используйте красивый веб-интерфейс
4. **При победе/проигрыше**:
   - Промокод отображается на сайте
   - Уведомление автоматически отправляется в Telegram бот
5. **Новая игра** - нажмите кнопку "Играть ещё раз" на сайте

## Особенности

- 🎮 **Два способа игры**: прямо в боте или на красивом сайте
- 🎯 Упрощенный ИИ (блокирует только в 50% случаев, дает шанс выиграть)
- 🎁 Генерация случайных 5-значных промокодов при победе
- ✨ Красивое отображение игрового поля (эмодзи в боте, градиенты на сайте)
- 🔄 Автоматическое обновление игрового поля после каждого хода
- 📱 Работает на всех устройствах
- 🔗 Автоматическая отправка уведомлений в Telegram при игре на сайте

## Технологии

- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **Backend**: NestJS, TypeScript
- **Telegram**: Telegraf.js

## Примечания

- Промокоды генерируются случайно и нигде не сохраняются
- Состояние игр в боте хранится в памяти сервера (при перезапуске сервера все активные игры сбросятся)
- При игре на сайте через `/site` - chatId передается через URL параметр для отправки уведомлений
- Если играете на сайте без chatId (напрямую открыли файл), уведомления в Telegram не отправятся
- Для работы бота нужен `TELEGRAM_BOT_TOKEN` в `.env` файле
- `FRONTEND_URL` нужен только если используете команду `/site`

